# Strike-Web

Онлайн-шутер в браузере в стиле Counter-Strike (CS 2 / CS:GO) на **Three.js**.
Подробный план проекта — в [PLAN.md](PLAN.md).

## Запуск

```bash
npm install
npm run dev
```

Сервер слушает `0.0.0.0:3000` и раздаёт статику из `client/`, Three.js — из `node_modules`,
а также REST API (`/api/*`) и WebSocket мультиплеер (`/ws`).

## Аккаунт и прогресс

- **Регистрация/вход** — пароль хранится хэшированным (scrypt + salt); сессия — cookie (HttpOnly),
  «запомнить меня» — localStorage.
- **Монеты** — старт 1000, +100 за убийство, тратятся на кейсы.
- **Кейсы** — 2 набора с редкостями (обычный → легендарный), выпавшие скины падают в инвентарь.
- **Данные** — `server/data/players.json` (в `.gitignore`, в git не коммитится).

## Мультиплеер

Другие игроки видны как фигуры с никами. Позиции синхронизируются (15 Гц), урон/смерти/респавн
обрабатывает сервер. Если WebSocket недоступен (например, в некоторых превью-окружениях) — игра
продолжает работать в одиночном режиме.

## Управление

- **WASD** — движение
- **Мышь** — обзор (клик захватывает курсор; если Pointer Lock недоступен — обзор зажатой ПКМ)
- **Space** — прыжок
- **Shift** — бег
- **ЛКМ** — огонь (AK-47 — держать зажатой)
- **ПКМ** — прицел (ADS): зум, точнее, тратит стамину (в fallback-режиме — клавиша **C**)
- **R** — перезарядка
- **1 / 2 / 3 / 4** — нож / Glock / Desert Eagle / AK-47 (или колесо мыши)
- **H** — тестовый урон (для проверки HP/смерти; уберём с врагами)
- **F1 / F2** — карты
- **M** / кнопка **⚙** — настройки
- **Esc** — выход из захвата курсора

## HP и стамина

- **HP** — полоса слева внизу. Урон от падения; смерть → экран «ТЫ УМЕР» и возрождение через 3 с.
- **Стамина** — бег (Shift) и прыжки тратят её, прицел тоже. Реген с задержкой, когда стоишь.
- **Вес оружия** — влияет на скорость: нож самый быстрый, AK-47 заметно медленнее.

## Оружие

| Оружие | Тип | Магазин |
|--------|-----|---------|
| 🔪 Нож | ближний бой, замах | — |
| 🔫 Glock-18 | полуавтомат | 20 / 60 |
| 💥 Desert Eagle | полуавтомат, мощный | 7 / 28 |
| 🪖 AK-47 | автомат (зажать ЛКМ) | 30 / 90 |

Вьюмодели — процедурные (без внешних 3D-моделей). Звук — синтез через WebAudio.

## Карты

| Карта | Что внутри |
|-------|------------|
| **Desert 2** | пустынная карта в стиле de_dust2: песок, мид, сайты, арки, ящики |
| **Травянистая пустошь** | трава, многоэтажки с ломающимися стёклами, контейнеры, старый дом-укрытие |

Переключение — кнопки в настройках или клавиши **1 / 2**.

## Настройки графики

Три пресета качества (переключаются в меню и применяются на лету):

| Пресет | Что включено |
|--------|--------------|
| **Низкое** | процедурные текстуры, без теней/травы/bloom, pixel ratio 1 |
| **Среднее** | детальные процедурные текстуры, тени, статичная трава |
| **Ультра** | bloom (пост-обработка), качающиеся трава и кактусы, текстуры в высоком разрешении |

Уровень определяется автоматически по железу (deviceMemory / ядра / мобильность).
> Примечание: «реальные текстуры из интернета» пока заменены процедурными (в окружении разработки внешние CDN закрыты) — загрузка PBR-текстур добавится позже.

## Структура

```
client/
  index.html            — входная точка
  css/style.css         — стили (HUD, меню настроек)
  js/
    main.js             — сборка сцены, запуск цикла, FPS
    config.js           — пресеты графики (Low/Medium/Ultra), авто-определение
    core/
      engine.js         — рендерер, сцена, камера, пост-обработка, цикл
      input.js          — клавиатура
      player.js         — контроллер от первого лица (физика, коллизии)
    graphics/
      textures.js       — процедурные текстуры (value-noise / fBm)
      postfx.js         — пост-обработка (bloom + tone mapping)
    weapons/
      data.js           — характеристики оружия
      controller.js     — логика: стрельба, перезарядка, отдача, вьюмодель
      models.js         — процедурные вьюмодели оружия
      effects.js        — вспышка, трассер, следы от пуль
      sound.js          — процедурные звуки (WebAudio)
    net/
      client.js         — WebSocket-клиент мультиплеера
      remote.js         — удалённые игроки (капсулы + ники, интерполяция)
    world/
      common.js         — база карты (свет/небо), коробки, стены с проёмами, арки, материалы
      breakables.js     — ломающиеся стёкла + осколки
      grass.js          — поле травы (InstancedMesh, шейдер ветра)
      cacti.js          — процедурные кактусы (качаются в Ultra)
      maps/
        index.js        — реестр карт
        desert2.js      — карта Desert 2
        wasteland.js    — карта «Травянистая пустошь»
    ui/
      hud.js            — HUD, оверлей старта
      settings.js       — панель настроек графики
      auth.js           — вход/регистрация
      menu.js           — меню-лобби (профиль, монеты)
      cases.js          — кейсы и инвентарь
    utils/random.js     — детерминированный PRNG
server/
  dev-server.js         — HTTP-сервер (статика + API + WebSocket)
  api.js                — REST API (auth, монеты, кейсы)
  ws.js                 — мультиплеер (WebSocket)
  store.js              — хранилище игроков (JSON) + сессии
  cases.js              — наборы кейсов и редкости
```
